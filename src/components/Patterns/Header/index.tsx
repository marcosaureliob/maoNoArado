'use client'

import React from 'react'

import { HeaderContainer } from '@/styles/page/header'

export const Header = () => {
  return (
    <HeaderContainer>
      <header>Aqui é o Header</header>
    </HeaderContainer>
  )
}
